An ncurses based clock that can display time (or anything else) in
kiloseconds (seconds since midnight / 1000)  or a custom format based
on the C strftime() function.

Supports colors, newlines and custom borders.

